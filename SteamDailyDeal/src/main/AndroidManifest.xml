<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gota.steamdailydeal" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name="com.gota.steamdailydeal.App"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <service
            android:name="com.gota.steamdailydeal.MyWidgetService"
            android:exported="false"
            android:permission="android.permission.BIND_REMOTEVIEWS" />

        <provider
            android:name="com.gota.steamdailydeal.data.DataProvider"
            android:authorities="com.gota.steamdailydeal.data.provider"
            android:exported="true" />

        <receiver android:name="com.gota.steamdailydeal.DailyDealWidget" >
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/daily_deal_widget_info" />
        </receiver>

        <activity
            android:name="com.gota.steamdailydeal.ConfigurationActivity"
            android:label="@string/title_activity_configuration" >
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <service
            android:name="com.gota.steamdailydeal.WorkService"
            android:exported="false" >
        </service>
    </application>

</manifest>
